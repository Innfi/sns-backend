import { IUserAccount } from '../src/persistence/model';
import { MockAccountAdapter } from '../src/persistence/mockAdapter';

describe('AccountAdapter', () => {
    it('mock adapter', () => {
        
    });
});